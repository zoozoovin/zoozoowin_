import '../../core/app_imports.dart';

class CustomPortionDivider extends StatelessWidget {
  const CustomPortionDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: AppColors.grey,
    );
  }
}
